# NOTE: Dependencies are now managed in pyproject.toml [project] section
# This file is kept for backward compatibility with older tooling
# To install: pip install -e . or pip install -e ".[dev]"

fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic==0.42.0
python-multipart==0.0.20
aiosqlite==0.20.0
sqlalchemy[asyncio]==2.0.36
greenlet==3.1.1
