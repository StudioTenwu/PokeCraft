# NOTE: Dependencies are now managed in pyproject.toml [project.optional-dependencies] section
# This file is kept for backward compatibility with older tooling
# To install: pip install -e . or pip install -e ".[dev]"

# Development dependencies (testing, linting, formatting)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.25.2  # For testing FastAPI endpoints
black==23.11.0
isort==5.12.0
ruff==0.1.6
mypy==1.7.1
pre-commit==3.5.0
